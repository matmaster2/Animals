package com.animals.animals.flying_animals;

import com.animals.animals.Animal;

abstract class FlyingAnimal extends Animal {
    FlyingAnimal(String name, int weight, int moveSpeed) {
        super(name, weight, moveSpeed);
    }
}
